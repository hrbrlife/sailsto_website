{
  "name": "hugo-site",
  "version": "1.0.0",
  "description": "This is the Hugo-powered version of the Sails.to website with complete 1:1 parity with the original HTML version.",
  "main": "index.js",
  "scripts": {
    "dev": "tinacms dev -c \"hugo server\"",
    "build": "tinacms build --local --skip-cloud-checks && hugo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tinacms/cli": "^2.1.6",
    "tinacms": "^3.5.0"
  }
}
